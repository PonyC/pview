import p{{properCase name}} from './src/{{name}}.vue'

p{{properCase name}}.install = Vue => {
   Vue.component(p{{properCase name}}.name,p{{properCase name}})
 }
 
export default p{{properCase name}}